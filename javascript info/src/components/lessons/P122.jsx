import Common from "../Common";
import Empty from "../Empty";

export default function P122({ title }) {
    const obj = {
        title: title,
    };

    return <Empty msg="Up Next..." />; //<Common obj={obj} />;
}
